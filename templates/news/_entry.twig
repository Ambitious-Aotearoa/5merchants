{% extends '_layout' %}
{% set bodyAttributes ={ class: "news-entry" } %} 
{% set docTitle = entry.title %} 



{% block content %} 
        <div class="conatined-content py-4  py-sm-vw-sm py-md-vw-xs ">
        <div class="contain d-flex flex-column flex-sm-row">
            <div class="col col-12 col-sm-4">
                {% if entry.title %} 
                    <h1>{{entry.title}}</h1>
                    <p>{{ entry.postDate | date }}</p>
                {% endif %} 

            </div>
             <div class="col col-12 col-sm-8">
                {% if entry.bodyText %} 
                    <div>{{entry.bodyText}}</div>
                {% endif %}
             </div>
        </div>
    </div>
{% endblock %}